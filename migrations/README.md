# Deprecated migrations directory

These historical files are retained only to explain installations created before Task 1. They are not executed. Their compatible effects are reconciled by the non-destructive canonical migrations in `database/migrations/`.
