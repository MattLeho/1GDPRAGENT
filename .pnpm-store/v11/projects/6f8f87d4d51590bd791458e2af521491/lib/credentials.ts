/** Shape accepted by the profile-scoped ONSIT credential settings route. */
export interface APICredentials {
    hibpApiKey?: string;
    hunterApiKey?: string;
    shodanApiKey?: string;
    whoisApiKey?: string;
}
