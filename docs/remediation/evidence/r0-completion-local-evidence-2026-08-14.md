# R0 completion local evidence — 2026-08-14

**Base commit:** `0ae187bff5e313ab528b758efc528ebf636c5088`
**Runtime:** Windows host with application/test phases isolated in existing Docker services and disposable `r0_browser_*` PostgreSQL databases.

This is local worktree evidence. The complementary successful hosted execution and downloaded artifact inspection are recorded in `r0-completion-hosted-evidence-2026-08-14.md`.

## Executed results

| Gate | Result |
|---|---|
| Compose configuration | pass with four purpose-distinct non-production sentinel keys |
| Migration histories | 11 passed; every successful history family applied twice with complete schema-object equality and representative rows survived |
| Python complete suite | 553 passed, 1 skipped, 9 warnings in 294.23s |
| Frontend TypeScript | pass |
| Frontend lint | 0 errors, 114 warnings |
| Frontend unit/component | 130 passed, 1 skipped |
| Frontend production build | pass; 62 static pages generated |
| Static/security suite | 70 passed, 6 warnings in 94.96s |
| Authenticated production browser | 7 ordinary passes plus strict expected `OPS-001` failure = 8 suite passes; 4 explicitly guarded R1 cases skipped |
| Browser artefacts | 8/8 journey directories each contain trace, automatic screenshot, named journey screenshot and named console/network JSON; 56-file HTML report and 9,844-byte JUnit |
| Focused CI/architecture contract | 18 passed, 1 Windows-only hosted-Linux regression skip |
| Clean frontend bootstrap | Node 22 + pnpm 11.9 frozen install passed in a disposable container; 1,054 packages and all declared lifecycle decisions applied |
| R0 runner continuation | disposable Linux regression ran gates with statuses `[1,0]`, executed the later gate and parsed `r0-gates.json` |

The Windows working tree uses `core.autocrlf=true`, so its bound shell files could not execute directly (`pipefail\r`). `git ls-files --eol` confirms the indexed scripts are LF, and the Git-normalised runner passed the Linux continuation/JSON regression. The exact complete wrapper remains mandatory hosted-Linux evidence.

## Browser runtime facts

- The app ran from `next start` using the production build, never from Fast Refresh.
- Global setup registered one disposable account, resolved its exact `default_profile_id`, and inserted one profile-owned request.
- Authenticated API probes used browser `fetch`. Trace inspection proved that Playwright's standalone API context omitted the `Secure` cookie on plain HTTP while Chromium's loopback browser context sent it.
- All unexpected browser `console.error` messages fail the responsible R0 test after screenshot/network evidence capture. The sole allowlist is the exact 401 resource message deliberately generated by `R0-AUTH-001`.
- The graph and graph-stats routes returned deterministic authenticated R0 test responses without contacting Neo4j.
- The literal `System Online` status remains a strict expected `OPS-001` failure assigned to R5/R7; R0 records rather than repairs it.

## Full-suite prerequisite facts

The first full run produced 544 passes, 1 skip and five failures:

- three graph integration tests could not resolve the stopped Neo4j service;
- two cross-runtime tests used a hard-coded Windows Codex Node path inside Linux.

After declaring Neo4j in CI and resolving Node from `PATH`, the graph cases passed. The final authoritative Docker run installed the declared Python requirements and Node only inside one disposable container, mounted the complete frontend dependency volume, passed the two cross-runtime cases, then passed the complete 553-test Python suite. The container removed itself after execution.

## Host CPU incident and staggering evidence

The 86.1% Microsoft Defender event was reproduced during the host `pnpm install`: the interrupted operation created/linked 1,055 package directories under `frontend/node_modules/.pnpm`. After it was stopped, package-manager wrappers were not used again for host execution. The auto-edited workspace approval placeholders and generated root `.pnpm-store` were removed.

Subsequent migrations, test phases and browser runs were executed sequentially. The immediate five-second Defender sample after the Docker production build was `13.96%, 3.08%, 3.09%, 1.53%, 3.10%`; no second host-install spike occurred.

## Evidence boundary

- `MODEL-008` remains an active, exact expected static finding in `intelligence/llm/gemini.py`, assigned to R3.
- The historical audit commit remains preserved in the issue registry.
- Four fresh independent read-only audits are recorded beside this file and every valid R0 finding was repaired and retested.
- Hosted run 31833659810 subsequently passed all nine gates; its uploaded manifest and browser artifacts were inspected independently of the workflow badge.
